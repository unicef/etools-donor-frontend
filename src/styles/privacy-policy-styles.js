import {
  makeStyles
} from "@material-ui/core";

export const usePrivacyPolicyStyles = makeStyles({
  root: {
    marginLeft: '10%',
    maxWidth: '80%'
  },
  title: {
    fontSize: '28px'
  }
});
